{
  "name": "race-dashboard-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that gates writes to the race-dashboard hub with username/password sessions.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node test/creation-grant.mjs && node test/read-cache.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  },
  "engines": {
    "node": ">=22"
  }
}
