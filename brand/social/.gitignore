# Working files only. The finished set is committed so it can be reached from
# the web (github.com, or sendoff.run/brand/social/) without a local render.
_strip.png
_pin-sheet.png
_prev-*.png
_cand-*.png
